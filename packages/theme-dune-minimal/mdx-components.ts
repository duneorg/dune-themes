/**
 * Baseline MDX components. NOTE: Dune loads mdx-components.ts only from the
 * active theme — child themes must re-export this file to keep these
 * components available (see README.md, rule 4).
 */
import Callout from "./components/callout.tsx";

export default { Callout };
