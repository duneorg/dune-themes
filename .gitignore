.claude/
.deno/
dist/
*.zip
.cache/
.DS_Store
notes/
demos/**/.dune/
demos/**/data/
# content/ is generated by `deno task demo:link` (copied from demos/_shared/*
# plus a generated About page from the theme's README) — not a symlink
# anymore since Dune's content walker doesn't follow symlinked directory
# entries. Regenerate with demo:link after checkout; CI does this before
# demo:validate.
demos/**/content/
